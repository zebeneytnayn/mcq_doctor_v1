# MCQ Doctor

A medical MCQ practice platform for students and professionals.

## Getting Started

Run `npm i` to install dependencies.

Run `npm run dev` to start the development server.

Run `npm run build` to build for production.